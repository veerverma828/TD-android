// Source indicator colors — deliberately not theme-derived, since they encode data
// provenance (local vs Trakt) rather than the active palette's accent.
export const CONTINUE_WATCHING_SOURCE_COLORS = {
  local: '#3B82F6', // blue
  trakt: '#E5484D', // red
} as const;
